name: QA

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - run: npm install --no-audit --no-fund
      - run: npm run typecheck
      - run: npm run typecheck:tests
      - run: npm test
      - run: npm run layout:check
      - run: npm run privacy:check
      - run: npx expo-doctor
      - run: npm run audit:runtime
      - run: npx expo export --platform android --output-dir dist-qa
      - run: npx expo prebuild --platform android --no-install --clean
      - run: node scripts/check-android-permissions.mjs
      - run: cd android && ./gradlew assembleDebug --no-daemon
      - run: node scripts/check-android-permissions.mjs --merged
      - uses: actions/upload-artifact@v4
        with:
          name: talktwo-qa-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
